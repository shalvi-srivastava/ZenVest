{
    "name": "zenvest",
    "private": true,
    "scripts": {
        "build": "npm --prefix frontend/landing install && npm --prefix frontend/landing run build && npm --prefix frontend/dashboard install && npm --prefix frontend/dashboard run build",
        "start": "node backend/index.js"
    }
}